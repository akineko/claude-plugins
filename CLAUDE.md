# CLAUDE.md

## リポジトリ概要

自作の Claude Code プラグインマーケットプレイスリポジトリ。リポジトリルートの `.claude-plugin/marketplace.json` がカタログとして全プラグインを管理し、各プラグインはサブディレクトリとして配置される。

## アーキテクチャ

- **マーケットプレイス定義**: `.claude-plugin/marketplace.json` — プラグイン一覧を `plugins` 配列で管理
- **各プラグイン**: `<plugin-name>/` ディレクトリ配下に以下を配置
  - `.claude-plugin/plugin.json` — メタデータ（`name`, `version` 必須）
  - `commands/`, `agents/`, `skills/`, `hooks/`, `.mcp.json` — 必要に応じて追加

## プラグイン追加・更新ワークフロー

1. `<plugin-name>/.claude-plugin/plugin.json` を作成（`name` と `version` を記載）
2. ルートの `.claude-plugin/marketplace.json` の `plugins` 配列にエントリ追加
3. 更新時は `plugin.json` の `version` をセマンティックバージョニングで上げる（更新検知に必要）

## ファイル操作の対象スコープ（重要）

このリポジトリはプラグインを**開発する**リポジトリであり、開発支援にも Claude Code のスキル・エージェントを利用している。同名・類似名のファイルが以下の3レイヤーに存在しうる。

| レイヤー | パス | 役割 |
|---------|------|------|
| 開発対象 | `<plugin-name>/skills/`, `<plugin-name>/agents/` | 開発中のプラグイン成果物 |
| プロジェクト | `.claude/skills/`, `.claude/agents/` | 開発支援用ツール |
| ユーザー | `~/.claude/skills/`, `~/.claude/agents/` | 個人の汎用ツール |

- プラグイン名が指示に含まれている場合、`<plugin-name>/` 配下のみを対象とする
- 複数レイヤーに該当ファイルがある場合、どのレイヤーが対象か確認してから作業する
- プロジェクト・ユーザーレベルのファイルは明示指定がない限り編集しない
- 参照元と編集先のレイヤーを混同しない

## プラグイン間の参照スコープ

プラグインごとに独自性を保ち、別バージョン（別プラグイン）への分岐を容易にするため、スキル・エージェント・コマンドを新規作成・編集する際の参照範囲を次のように制限する。

- **同一プラグイン内**（`<plugin-name>/` 配下）の成果物は自由に参照してよい。スキル間の役割分担や、別スキルの出力を前提とした連携設計は積極的に行う。
- **他プラグイン**（`<other-plugin>/` 配下）の成果物は、ユーザーが明示的に「<プラグイン名> の <スキル/エージェント名> を参考に」と指示した場合のみ参照する。
- ここでの「参照」には、構成・記述スタイル・出力フォーマット・トリガーフレーズの流用も含む。

## 手動呼び出しスキルの frontmatter 規約

`/<skill-name>` での手動起動のみを想定するスキル（自動起動させたくないもの）は、frontmatter を次のように設定する。

- **`disable-model-invocation: true`** を設定し、モデルによる自動起動を止める（手動起動のみにする）。
- **`description` にトリガーフレーズを列挙しない**。自動起動しない以上トリガーフレーズ（「レビューして」等）は役割を失い、補完メニューの説明文を冗長にして読みづらくするだけになる。代わりに「何をするスキルか＋兄弟スキルとの棲み分け」を簡潔な一文で書く（補完メニューでの可読性と識別性を優先する）。
- **`argument-hint`** を設定し、引数の形式・既定値を補完時に提示する。対象の指定方法など description から外した情報はここに置く（重複を避け 1 情報は 1 箇所）。省略可の引数は `[...]`、必須は `<...>` で表記する。

## スキル・エージェントの設計ノート（RATIONALE.md）

SKILL.md / エージェント定義は「何をするか（指示）」だけを書き、**「なぜその形か」は別ファイル `RATIONALE.md` に残す**。SKILL.md だけでは、ある指示が「消すと壊れる earned wisdom」なのか恣意的なのか、どんな代替案を却下したのか、元の意図は何だったかが読み取れず、更新・再設計時にその知見が失われるため。RATIONALE.md は**保守者向けドキュメントで、スキル実行時には読み込まれない**。

- **置き場所**: スキルは `<skill-name>/RATIONALE.md`（SKILL.md と並置）。エージェントは `agents/<name>.RATIONALE.md`（定義ファイルと対にする）。ただし `agents/` 配下の `.md` はエージェントとして自動登録される為、`plugin.json` の `agents` フィールドに定義ファイルのみを明示列挙し、RATIONALE.md の誤登録を防ぐ（列挙はデフォルトの `agents/` 全スキャンを置き換えるので、エージェント追加時は列挙も更新する）。
- **構成**: 4セクション（`目的と背景` / `中核的な設計判断` / `解釈・仮定（要確認）` / `改訂ログ`）の決定中心の living doc。改訂して最新を保ち、純粋な追記ログにはしない。**雛形・各セクションの役割・記入ガイドは `templates/RATIONALE.template.md` を唯一の出所とする**（ここには再掲しない）。
- **読むタイミング**: スキル/エージェントをゼロベース再設計・大幅改修する前に、対になる RATIONALE.md を先に読む。定義だけ見て作り直すと earned wisdom が抜け落ちる（このルールが存在する理由そのもの）。
- **書くタイミング**: 新規作成時に初期化（目的・背景・初期の主要判断・解釈）。更新時は該当する判断/解釈を**改訂**し、大きな方針転換なら改訂ログに1行。作成・更新で曖昧点を質問して確定したら、その解釈を「解釈・仮定」に記録する。
- **規律**: SKILL.md の内容は再掲せず参照に留める（重複は更新漏れの元）。AI が起案し、ユーザーがレビューして意図とのズレを正す。

## スキルの動作確認（eval）成果物の扱い（Git 運用）

skill-creator でスキルを検証すると、テスト定義・入力データ・実行成果物が生成される。**コミットするのはテスト定義（`evals.json`）だけ**で、動作確認に使うコードは全て workspace に閉じ込めて追跡しない。判断軸は「スキルの一部か／動作確認のために用意した使い捨てのコードか」。

| 種類 | 置き場所 | Git | 理由 |
|------|---------|-----|------|
| テスト定義 | `<skill>/evals/evals.json` | **コミット** | スキルのテストスイート本体。再設計時に「同じ eval で parity 確認」する基準になる（[[RATIONALE.md]] が「なぜ」を残すのと同じく「どう検証したか」を残す earned wisdom） |
| 入力データ・実行成果物 | `<skill>-workspace/` | **gitignore**（`*-workspace/`） | 入力（上流スキルの出力・監査対象のダミープロジェクト等）も iteration/grading/benchmark 等の出力も、すべて動作確認のために用意した使い捨てコード。再生成でき、プラグインと無関係な大量コードを履歴に持ち込まない。gitignore はローカルのファイルを消さないので差分確認には引き続き使える |

- **動作確認に使うコードは全て `*-workspace/` 命名のディレクトリに置く**。単一スキル専用なら `<skill>-workspace/`、複数スキルで共有する検証コードベース（例: パイプライン検証用のサンプルアプリ）は特定スキルに属させず中立の `_codebases-workspace/` 等に置く。スキル本体ディレクトリ（`<skill>/`）に置くのは `evals.json` などのテスト定義のみ。入力データを `fixtures/` のように別概念で切り出さない（切り出すと ignore 対象が増え、再生成可能なものを永続化する誘惑が生まれる）。これにより ignore は `*-workspace/` の1パターンで完結する（skill-creator が規定する固定名なので都度追加も不要）。
- **`evals.json` の `prompt`・`files` は workspace 内の入力をリポジトリルート相対パスで参照する**（実行 cwd に依存せず一意に解決できるため）。入力実体はコミットしないので、他環境では eval 実行前に入力を用意する（上流スキルの eval を実行して出力を得る／監査対象のダミープロジェクトを再生成する等）。
- **再生成にコストがかかる手書き入力**（例: 正解データと対になる脆弱性サンプル）が workspace にある場合、workspace を削除すると失われる点に注意（必要ならローカルで退避）。それでも「スキル本体ではない」ので追跡対象にはしない。
