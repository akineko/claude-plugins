# CLAUDE.md

## リポジトリ概要

自作の Claude Code プラグインマーケットプレイスリポジトリ。リポジトリルートの `.claude-plugin/marketplace.json` がカタログとして全プラグインを管理し、各プラグインはサブディレクトリとして配置される。

## アーキテクチャ

- **マーケットプレイス定義**: `.claude-plugin/marketplace.json` — プラグイン一覧を `plugins` 配列で管理
- **各プラグイン**: `<plugin-name>/` ディレクトリ配下に以下を配置
  - `.claude-plugin/plugin.json` — メタデータ（`name`, `version` 必須）
  - `commands/`, `agents/`, `skills/`, `hooks/`, `.mcp.json` — 必要に応じて追加

## プラグイン追加・更新ワークフロー

1. `<plugin-name>/.claude-plugin/plugin.json` を作成（`name` と `version` を記載）
2. ルートの `.claude-plugin/marketplace.json` の `plugins` 配列にエントリ追加
3. 更新時は `plugin.json` の `version` をセマンティックバージョニングで上げる（更新検知に必要）

## ファイル操作の対象スコープ（重要）

このリポジトリはプラグインを**開発する**リポジトリであり、開発支援にも Claude Code のスキル・エージェントを利用している。同名・類似名のファイルが以下の3レイヤーに存在しうる。

| レイヤー | パス | 役割 |
|---------|------|------|
| 開発対象 | `<plugin-name>/skills/`, `<plugin-name>/agents/` | 開発中のプラグイン成果物 |
| プロジェクト | `.claude/skills/`, `.claude/agents/` | 開発支援用ツール |
| ユーザー | `~/.claude/skills/`, `~/.claude/agents/` | 個人の汎用ツール |

- プラグイン名が指示に含まれている場合、`<plugin-name>/` 配下のみを対象とする
- 複数レイヤーに該当ファイルがある場合、どのレイヤーが対象か確認してから作業する
- プロジェクト・ユーザーレベルのファイルは明示指定がない限り編集しない
- 参照元と編集先のレイヤーを混同しない

## プラグイン間の参照スコープ

プラグインごとに独自性を保ち、別バージョン（別プラグイン）への分岐を容易にするため、スキル・エージェント・コマンドを新規作成・編集する際の参照範囲を次のように制限する。

- **同一プラグイン内**（`<plugin-name>/` 配下）の成果物は自由に参照してよい。スキル間の役割分担や、別スキルの出力を前提とした連携設計は積極的に行う。
- **他プラグイン**（`<other-plugin>/` 配下）の成果物は、ユーザーが明示的に「<プラグイン名> の <スキル/エージェント名> を参考に」と指示した場合のみ参照する。
- ここでの「参照」には、構成・記述スタイル・出力フォーマット・トリガーフレーズの流用も含む。

## スキル・エージェントの設計ノート（RATIONALE.md）

SKILL.md / エージェント定義は「何をするか（指示）」だけを書き、**「なぜその形か」は別ファイル `RATIONALE.md` に残す**。SKILL.md だけでは、ある指示が「消すと壊れる earned wisdom」なのか恣意的なのか、どんな代替案を却下したのか、元の意図は何だったかが読み取れず、更新・再設計時にその知見が失われるため。RATIONALE.md は**保守者向けドキュメントで、スキル実行時には読み込まれない**。

- **置き場所**: スキルは `<skill-name>/RATIONALE.md`（SKILL.md と並置）。エージェントは `agents/<name>.RATIONALE.md`（定義ファイルと対にする）。
- **構成**: 4セクション（`目的と背景` / `中核的な設計判断` / `解釈・仮定（要確認）` / `改訂ログ`）の決定中心の living doc。改訂して最新を保ち、純粋な追記ログにはしない。**雛形・各セクションの役割・記入ガイドは `templates/RATIONALE.template.md` を唯一の出所とする**（ここには再掲しない）。
- **読むタイミング**: スキル/エージェントをゼロベース再設計・大幅改修する前に、対になる RATIONALE.md を先に読む。定義だけ見て作り直すと earned wisdom が抜け落ちる（このルールが存在する理由そのもの）。
- **書くタイミング**: 新規作成時に初期化（目的・背景・初期の主要判断・解釈）。更新時は該当する判断/解釈を**改訂**し、大きな方針転換なら改訂ログに1行。作成・更新で曖昧点を質問して確定したら、その解釈を「解釈・仮定」に記録する。
- **規律**: SKILL.md の内容は再掲せず参照に留める（重複は更新漏れの元）。AI が起案し、ユーザーがレビューして意図とのズレを正す。
