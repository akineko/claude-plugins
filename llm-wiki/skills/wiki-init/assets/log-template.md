# Log

Wiki に対する操作の年代記。**追記専用**で、過去のエントリを書き換えない。

エントリのプレフィックスは Unix ツールでパースできる形式に揃える:

```
## [YYYY-MM-DD] {action} | {title}
```

`action` の値: `init` / `ingest` / `query` / `lint` / `schema` / `note`

---
