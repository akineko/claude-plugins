# Revision エージェント用プロンプトテンプレート

SKILL.md の Phase 4（レビュー）で使用する。ユーザーのフィードバックに基づき、設計書の特定セクションのみを再設計する。
`{現在の設計書}` と `{ユーザーのフィードバック}` と `{修正対象セクション}` と `{コードベース情報}` を埋めて `greenfield-architect` に渡す。
`{コードベース情報}` には影響分析書（パスA）またはResearch結果（パスB）を入れる。

---

## タスク

以下の設計書に対してユーザーからフィードバックがあった。指定されたセクションのみを修正せよ。
他のセクションは変更するな。

### ユーザーのフィードバック

{ユーザーのフィードバック}

### 修正対象セクション

{修正対象セクション}

### 現在の設計書

{現在の設計書}

### コードベース情報（影響分析書またはResearch結果）

{コードベース情報}

---

## 出力フォーマット

修正対象セクションのみを出力せよ。セクション番号とタイトルを含める。
未修正セクションは出力に含めるな。

フォーマットは `design-prompt.md` の各セクション定義に従う。

## 制約

- フィードバックで指摘された点のみを修正する。無関係な改善を加えるな
- 修正により他セクションとの整合性が崩れる場合、影響を受けるセクションも修正対象に含め、その旨を出力の冒頭に記載せよ
- 出力完了前に、フィードバックの全指摘事項が反映されているか自己検証せよ
