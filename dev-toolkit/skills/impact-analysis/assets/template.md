# 影響分析書: {タイトル}

**対象要求仕様書**: `{パス}`

## 1. 変更一覧

要求仕様書の要件を変更種別ごとに分類する。
変更内容は要件レベルで記述し、実装手段には言及しない。

| ID | 種別 | 変更内容 | 要求仕様の根拠 |
|----|------|---------|--------------|
| C1 | 追加 | ... | 要求X |
| C2 | 変更 | ... | 要求Y |
| C3 | 削除 | ... | 要求Z |

## 2. 影響範囲

変更ごとに、調査で判明した影響を記載する。

- 「変更対象ファイル」には既存ファイルのみ記載する。新規ファイル名・ディレクトリ名の提案は設計の責務であり、ここには含めない
- 「必要な変更」列には変更の性質と対象を書く（例: 「商品用データ取得の追加」「legacyFlagフィールドの削除」）。具体的なコンポーネント名・関数名は書かない

### C1: {変更内容}

#### 変更対象ファイル

| ファイル | 現在の役割 | 必要な変更 |
|----------|-----------|-----------|

#### 参考にすべき既存パターン

| ファイル | パターンの概要 |
|----------|--------------|

#### 注意点

- {この変更に固有の注意点}

### C2: {変更内容}

(同じ構造を繰り返す)

## 3. 設計制約

設計時に考慮すべき事実と制約を記載する。
選択肢・推奨案・設計判断は含めない。

### 技術的制約

- {フレームワーク・既存アーキテクチャ由来の制約}

### ビジネス制約

- {要求仕様書に記載されている優先度・必須/任意の区別}

### 互換性制約

- {既存APIとの互換性、データマイグレーションの必要性}

### 横断的な注意点

- {複数の変更に共通する注意点}

## 4. 依存関係分析

### 変更間の依存

```
凡例: A → B は「AはBに依存する（Bが先に完了している必要がある）」

{依存グラフ（ASCII図）}
```

### 独立性の評価

| 評価基準 | {変更ペアA} | {変更ペアB} | ... |
|----------|-----------|-----------|-----|
| データモデルの独立性 | {同一テーブル/別テーブル等の事実} | | |
| デプロイの独立性 | {単独リリース可否とその根拠} | | |
| レイヤー依存の方向 | {API確定がUI設計の前提か等} | | |

## 5. 推奨設計単位

依存関係分析に基づき、独立して設計可能な単位を提案する。

### 単位1: {名称}

- **含む変更**: C1, C2
- **対象レイヤー**: backend, frontend, shared
- **スコープ概要**: {1-2文}

### 単位2: {名称}

(同じ構造を繰り返す)

### 設計単位間の実施順序

{順序の根拠は依存関係の事実（「AのAPIレスポンス型がBの入力に必要」等）のみ。効率性・合理性の判断は含めない}
