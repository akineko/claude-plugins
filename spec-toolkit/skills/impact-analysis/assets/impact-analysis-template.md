# 影響分析書: <機能名>

<!--
全セクションを必ず残す。該当する内容がない場合は、見出しの直下に「該当なし」と明記する。
理由: 後続スキル(implementation-design)はこの構造を前提に動くため、
「セクションが存在しない」と「セクションがあって該当なし」は意味が違う。

このスキルが扱うのは「何が変わるか / どこに当たるか / 何を設計する必要があるか / どの順で設計するか」まで。
具体的な How (テーブルスキーマ・API契約・関数シグネチャ・コードレイアウト) は書かない(implementation-design の担当)。
-->

## 1. 概要

- **対象機能設計書**: <機能設計書のパス>
- **対象要求**: <扱う要求IDのリスト (例: R1, R2, R3)>
- **制約条件(上流から)**: <機能設計書/要求仕様書で制約として与えられた技術指定・既存基盤指定など。どの要求(R)由来かを併記する。後続が「これは選べる設計判断か、上流で与えられた制約か」を冒頭で判別できるようにするための欄。なければ「該当なし」>
- **対象外**: <機能設計書にあるが本影響分析では扱わない要求があればその旨と理由に加え、差し戻し先を書く(対象外にした要求が静かに消えるのを防ぐ)。なければ「該当なし」>
- **調査範囲**: <impact-investigator サブエージェントによる調査を実施した範囲の要旨。実施していない場合は「コードベース調査は省略(理由を1行)」>

## 2. 変更点と影響範囲

<!--
機能設計書の機能(F)ごとに整理する。機能設計書 §4 の機能(F)単位を保持することで、
特定の要求に1:1で紐づかない前提機能の変更点も行き場を失わない。
論理的対象(「商品カードコンポーネント」)とコード上の具体パスを両方残すと、
implementation-design が読みやすい。
-->

### 2.1 F<n> <機能名>

- **関連要求**: <R1, R2 (機能設計書 §4 の関連要求を転記)>

**追加されるもの**:

- <論理的な対象X>: <何が追加されるか>

**変更されるもの**:

- <既存対象Y>: <どんな変更が必要か>

**波及確認が要る箇所**:

- <影響対象Z>: <どう影響を受けるか>

**コードベース上の関連箇所**:

| パス | 役割 | 関連度 (主/関連) |
|------|------|-----------------|
| <path/to/file.tsx> | <そのファイルが本機能でどう関わるか> | 主 |
| <path/to/other.ts> | <参考として把握すべき関連箇所> | 関連 |

<!-- 機能の数だけ 2.x セクションを繰り返す -->

## 3. 設計トピック

<!--
詳細設計フェーズで「設計対象」として扱うべきトピックを列挙する。
粒度の目安: 単独で1パスの設計案にまとまるサイズ(細かすぎる/大きすぎるは原則3を参照)。
トピック数の目安: 多くても 5〜7 個程度に収まることが多い。
小規模な機能なら 1〜2 個でよい。
8 個を超える場合は隣接トピックの統合余地を見直す(過剰分解は原則3の趣旨に反する)。
-->

### 3.1 T<n> <トピック名>

- **概要**: <このトピックで決める対象を1〜2行>
- **関連する機能/要求**: <F1 (R1, R2) のように、機能とその関連要求を併記>
- **関連する既存箇所**: <影響を与える/受ける既存コード箇所>
- **設計時に決めるべきこと**:
  - <問い1: 答えではなく問いの形で書く>
  - <問い2>

<!-- トピックの数だけ 3.x セクションを繰り返す -->

## 4. 設計バッチと設計順序

### 4.1 設計バッチ

<!--
設計トピックを「一度の設計セッションで一緒に検討するまとまり」に集約する。
バッチが1つの場合は「設計バッチは1つに集約した」とその理由を記す。
-->

#### B<n> <バッチ名>

- **含まれるトピック**: T1, T3
- **このバッチで一緒に決めること**: <なぜ一緒に決めるべきかを1〜2行>

<!-- バッチの数だけ繰り返す -->

### 4.2 設計順序

<!--
バッチ間の依存関係と推奨設計順序を整理する。
バッチが1つしかない場合はこのセクション自体に「設計バッチが1つのため順序の議論は不要」とだけ書く。
設計順序図は下記の2パターンのうち、機能の構造に近い方を選んで使う。
-->

<!-- パターン1: 直列のみ
```
B1 → B2 → B3
```
-->

<!-- パターン2: 直列+並列分岐 -->

```
B1
  ├─→ B2 ─→ B3
  └─→ B4 (B2 と並列で進めてよい)
```

- **推奨設計順序**: B1 → B2 → B3
- **各バッチを先にする理由**: <1〜2行>
- **並列に進められるバッチ**: <あれば>
- **設計順序が確定しない論点**: <あれば>

## 5. 後続フェーズへの引き継ぎ

<!--
implementation-design が詳細設計に入るときに留意してほしい論点を整理する。
-->

- **設計バッチ単位での実装設計を推奨**: 上記 4.1 のバッチ単位で実装設計書を分割するか、章分けすることを推奨する
- **<バッチX固有の引き継ぎ>**: <論点や注意点>
- **機能設計書の前提と現状コードの齟齬** (該当する場合): <発見された不整合と扱い方>
- **未確定論点** (該当する場合): <影響分析の中で確定しきれなかった点>
