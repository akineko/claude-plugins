# 実装設計書: <バッチ名>

<!--
全セクションを必ず残す。該当する内容がない場合は、見出しの直下に「該当なし」と明記する。

このファイルは spec-toolkit/skills/implementation-design スキルが影響分析書 (impact-analysis.md) の
1つの設計バッチを対象に生成する。1スキル実行 = 1バッチ。

設計骨格 → 詳細設計 → 実装計画 の順で詳細度が上がる構造。
- 骨格: 「何をどう捉えて何を決めたか」(構造)
- 詳細: 「具体的な契約と構造」(契約レベル)
- 実装計画: 「TDDフェーズ分割」(動くもの単位)

実装本体のコード・内部アルゴリズム擬似コード・呼び出し例は書かない。
理由: (1) 設計書の肥大化で実装フェーズに必要な情報を探しにくくなる、
(2) 読み手が設計レビューだけでなくコードレビューを要求される、
(3) 実装の自由度が不当に縮む。
-->

## 1. 概要

- **対象設計バッチ**: <B<n>: バッチ名> (影響分析書 4.1)
- **含まれる設計トピック**: <T1, T3 等> (影響分析書 3)
- **参照ドキュメント**:
  - 影響分析書: <path>
  - 機能設計書: <path>
  - 要求仕様書: <path>

## 2. 設計骨格

<!--
このバッチで決めた大きな構造を示すセクション。
詳細(具体的なシグネチャ・列定義・型注釈)は 3. 詳細設計 に書き、ここには書かない。
-->

### 2.1 主要な意思決定

- **<決定1>**: <要点>
  - **採用理由**: <要件の何に応えるか / どの評価軸で勝つか>
  - **却下した主要案**: <案A: 概要 / 却下理由> (なければ「該当なし」)

<!-- 決定の数だけ繰り返す -->

### 2.2 概念 / データモデル

業務概念・エンティティと関係性、不変条件。実装上の表現(具体的な列名・型)はここに書かない。

- **<概念A>**: <定義・責務>
- **<概念B>**: <定義・責務>
- **関係性**: <A は B を 0..N 持つ等>
- **不変条件**:
  - <ルール1>
  - <ルール2>

### 2.3 モジュール / 責務構造

責務をどこに置くか・依存方向。具体的なファイル/関数名ではなく、責務単位の構造。

- **<モジュールX>**: <責務>
- **<モジュールY>**: <責務>
- **依存方向**: X → Y (X が Y に依存)

### 2.4 設計トピック別方針

影響分析書の各トピック (T<n>) について、骨格段階で確定した方針を1〜2行ずつ。

- **T1 <トピック名>**: <方針>
- **T2 <トピック名>**: <方針>

<!-- トピックの数だけ繰り返す -->

## 3. 詳細設計

<!--
「実装フェーズで設計に立ち戻らずに済む契約レベル」を満たす範囲で書く。

書くもの:
- データ構造 / DB スキーマ: 列・型・制約・インデックス
- API / 関数インタフェース: シグネチャ・リクエスト/レスポンス形
- 主要な動作フロー: 入力 → 処理 → 出力の流れ (5ステップ以内)
- エラーハンドリング / エッジケース: 条件と対応方針

書かないもの:
- 実装本体のコード
- 内部アルゴリズムの擬似コード
- 呼び出し例
-->

### 3.1 データ構造 / DB スキーマ

- **<テーブル / 型名>**: <用途>
  - フィールド: <名: 型 (制約)>
  - インデックス: <あれば>
  - 制約: <一意制約・外部キー等>

<!-- データ構造の数だけ繰り返す -->

### 3.2 API / 関数インタフェース

モジュール境界の契約を列挙する。

- **<エンドポイント / 関数名>**: <用途>
  - リクエスト/引数: <形>
  - レスポンス/戻り値: <形>
  - 主要エラー: <型/ステータス>

<!-- インタフェースの数だけ繰り返す -->

### 3.3 主要な動作フロー

設計理解に不可欠なフローのみ。網羅は不要。

- **<フロー名>**: <入力 → 処理 → 出力。5ステップ以内>

### 3.4 エラーハンドリング / エッジケース

- **<エラー条件1>**: <対応方針>
- **<エッジケース2>**: <対応方針>

## 4. 実装計画

<!--
TDD ベースのフェーズ分割。各フェーズで「先に書くテスト」と「そのテストを通すための
実装範囲」を明示する。1フェーズ = 動くもの (テスト pass する単位) を端から端まで揃える。

「型だけ定義」「ファイルだけ作る」のような中間状態はフェーズにしない。
端から端まで通る最小単位を起点にし、そこから機能を増やす方向でフェーズを積む。
-->

### フェーズ 1: <フェーズ名>

- **目的**: <このフェーズで何が動くようになるか>
- **テスト方針**: <先に書くテスト: ユニット/統合/契約テスト など>
- **実装範囲**: <どのモジュール/契約を実装するか>
- **完了条件**: <該当テストが pass + 動作確認>

<!-- フェーズの数だけ繰り返す -->

## 5. 残課題・後続検討事項

<!--
本実装設計書の範囲外で別途検討すべき論点、または実装後に再評価が必要な事項。
なければ「該当なし」。
-->

- <論点1>
- <論点2>
