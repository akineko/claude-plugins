# 実装設計書: <バッチ名>

<!--
全セクションを必ず残す。該当する内容がない場合は、見出しの直下に「該当なし」と明記する。

このファイルは spec-toolkit/skills/implementation-design スキルが影響分析書 (impact-analysis.md) の
1つの設計バッチを対象に生成する。1スキル実行 = 1バッチ。

設計方針 → 確定仕様 → 実装計画 の順で読む構造。2章と3章の役割は情報の種類で分け、同じ情報を両章に書かない。
- 設計方針(2章): 「なぜこの形か」と全体の地図(決定・概念/不変条件・配置と依存)。契約の具体は書かない
- 確定仕様(3章): 実装に着手できる契約(設計対象ごとに契約・動作・エラーを一体で)。理由は書かない(D<n> 参照)
- 実装計画(4章): 「フェーズ分割」(動くもの単位)

実装本体のコード・内部アルゴリズム擬似コード・呼び出し例は書かない。
理由: (1) 設計書の肥大化で実装フェーズに必要な情報を探しにくくなる、
(2) 読み手が設計レビューだけでなくコードレビューを要求される、
(3) 実装の自由度が不当に縮む。
-->

## 1. 概要

- **対象設計バッチ**: <B<n>: バッチ名> (影響分析書 4.1)
- **全バッチ中の位置**: <例: B1, B2, B3 のうち本書は B2。このフェーズはバッチ数だけ複数回実行する点を利用者が見失わないよう明記する>
- **含まれる設計トピック**: <T1, T3 等> (影響分析書 3)
- **制約条件(上流から)**: <影響分析書/上流で制約として与えられた技術指定。どの要求(R)由来か。なければ「該当なし」>
- **参照ドキュメント**:
  - 影響分析書: <path>
  - 機能設計書: <path>
  - 要求仕様書: <path>
  - 先行バッチの実装設計書: <あれば path。バッチ間整合のため確定事項を前提にする>

## 2. 設計方針

<!--
「なぜこの形か」と全体の地図を示す章。読み手はここだけで設計の方針と判断根拠を掴める。
契約の具体(シグネチャ・列定義・パス形式・具体値)は 3. 確定仕様 に書き、ここには書かない。
-->

### 2.1 主要な意思決定

- **D1: <決定名>**: <要点>
  - **採用理由**: <要件の何に応えるか / どの評価軸で勝つか>
  - **却下した主要案**: <案A: 概要 / 却下理由> (なければ「該当なし」)

<!-- 決定の数だけ D1, D2, … と連番で繰り返す。ID は 2.4 や後続バッチが決定を参照するためのアンカー -->

### 2.2 概念 / データモデル

業務概念・エンティティと関係性、不変条件。実装上の表現(具体的な列名・型)はここに書かない。
不変条件はここが唯一の定義箇所 — 3章で再説明せず、3章には違反の検知・対処だけを書く。

- **<概念A>**: <定義・責務>
- **<概念B>**: <定義・責務>
- **関係性**: <A は B を 0..N 持つ等>
- **不変条件**:
  - <ルール1>
  - <ルール2>

### 2.3 モジュール / 責務構造

何がどこに置かれ(新設/変更の別)、どちらに依存するかの「配置の地図」。
配置先は既存の実パスで書いてよいが、処理内容・シグネチャ・クエリ条件は書かない(3章の情報)。

- **<モジュールX>**: <責務>
- **<モジュールY>**: <責務>
- **依存方向**: X → Y (X が Y に依存)

### 2.4 設計トピック別方針

影響分析書の各トピック (T<n>) が、どの意思決定 (2.1 の D<n>) でどう解決されるかの対応付け。
決定内容を再掲せず、決定への参照と、どの決定にも属さない補足だけを1〜2行で書く
(2.1 と同じ情報を二重に持つと、肥大化と改訂時の更新漏れを招く)。

- **T1 <トピック名>**: <解決する決定への参照(例: D1・D3)＋決定に現れない補足があれば1行>
- **T2 <トピック名>**: <同上>

<!-- トピックの数だけ繰り返す -->

## 3. 確定仕様

<!--
実装フェーズがこの章だけで実装に着手できる契約を書く。

- 理由・却下案・経緯は書かない。必要なら (D2) のように 2.1 の決定を参照する。
- 設計対象の自然な単位(バックエンド: エンティティ/エンドポイント群/ハンドラ、
  フロントエンド: コンポーネント/フック 等)ごとに ### 節を切り、
  1つの対象の情報を1箇所に集める。分類のために1つの対象を複数の節へ分けない。
- 各対象で「契約・動作・エラー」の3点を点検し、該当するものを書く。
- 書くもの: 型・列・制約・インデックス / シグネチャ・リクエスト/レスポンス形・主要エラー /
  設計理解に不可欠な動作フロー(5ステップ以内) / エラー・エッジケースの条件と対処
- 書かないもの: 実装本体のコード / 内部アルゴリズムの擬似コード / 呼び出し例
- 列定義など長い列挙になる契約は表を使ってよい。
-->

### 3.1 <設計対象A (例: エンティティ<名前> / <リソース>の公開エンドポイント / <コンポーネント名>)>

- **契約**: <型・列定義・制約・インデックス / シグネチャ・リクエスト/レスポンス形・主要エラー / props と責務>
- **動作**: <設計理解に不可欠なフローのみ。入力 → 処理 → 出力で5ステップ以内。なければ省略>
- **エラー・エッジケース**: <条件と対処。2.2 の不変条件は再説明せず、違反の検知・対処だけ。なければ省略>

#### ライフサイクル(新規エンティティの場合)

<!--
レコードがいつ作られ・更新され・消えるかを一望する表。
削除は直接削除に加え、DB スキーマのカスケード定義を祖先方向に遡った全経路を1行ずつ列挙する。
各契機は要求(R)または上流決定に遡れること(遡れない契機は YAGNI 候補としてユーザー確認に載せる)。
-->

| 契機 | レコード | 付随アセット(ファイル等) |
|---|---|---|
| 作成: <操作/経路> | <何が起きるか> | <あれば> |
| 更新: <操作ごと> | | |
| 削除: <直接削除> | | |
| 削除: <カスケード元ごとに1行> | | |

<!-- 設計対象の数だけ 3.2, 3.3, … と節を繰り返す -->

### 3.<n> 横断事項 (該当する場合のみ、最後に置く)

<!-- 単一の設計対象に属さない事項だけをここに集める -->

- **共有スキーマ/型の変更の波及**: <変更するスキーマと、それを返す全経路(単一取得・一覧・mutation・公開・複製 等)ごとの対応>
- **対象をまたぐ動作フロー / エラー方針**: <あれば>

## 4. 実装計画

<!--
動くもの単位のフェーズ分割。各フェーズで「完了を固定するテスト」と「その実装範囲」を明示する。
1フェーズ = 動くもの (テスト pass する単位) を端から端まで揃える。

「型だけ定義」「ファイルだけ作る」のような中間状態はフェーズにしない。
端から端まで通る最小単位を起点にし、そこから機能を増やす方向でフェーズを積む。
テストを先に書くか後に書くかなどの実装プロセスは実装者の裁量であり、ここでは規定しない。
-->

### フェーズ 1: <フェーズ名>

- **目的**: <このフェーズで何が動くようになるか>
- **テスト方針**: <このフェーズの完了を固定するテスト: ユニット/統合/契約テスト など>
- **実装範囲**: <どのモジュール/契約を実装するか>
- **完了条件**: <該当テストが pass + 動作確認>

<!-- フェーズの数だけ繰り返す -->

## 5. 残課題・後続検討事項

<!--
本実装設計書の範囲外で別途検討すべき論点、または実装後に再評価が必要な事項。
なければ「該当なし」。
-->

- <論点1>
- <論点2>
