# 機能設計書: <機能名>

<!--
全セクションを必ず残す。該当する内容がない場合は、見出しの直下に「該当なし」と明記する。
理由: 後続スキル(impact-analysis 等)はこの構造を前提に動くため、
「セクションが存在しない」と「セクションがあって該当なし」は意味が違う。

このスキルが扱うのは「ユーザーから見た機能の振る舞い」だけ。
ファイル名・テーブル設計・API・ライブラリなど実装目線の情報は書かない(後続スキルの担当)。
-->

## 1. 概要

- **対象要求仕様書**: <要求仕様書のパス>
- **対象範囲**: <この機能設計書で扱う要求IDのリスト。要求仕様書の 3.x を R3.x と表記し、本文の `関連要求` と揃える(例: R3.1, R3.2)>
- **制約条件(上流から)**: <request-spec の各要求の「制約条件」由来の技術指定・既存基盤指定など。どの要求(R番号)由来かを併記する。後続が「これは選べる設計判断か、上流で与えられた制約か」を冒頭で判別できるようにするための欄。なければ「該当なし」>
- **対象外**: <要求仕様書にあるが、このスキルでは扱わない要求があればその旨と理由に加え、**差し戻し先**を必ず書く(例: R5 — 優先度低。次サイクルの request-spec で再評価予定 / 別 docs/plans/<別機能>/ で扱う)。対象外にした要求が静かに消えるのを防ぐため。なければ「該当なし」>
- **要求カバレッジ**: <全要求がいずれかの機能でカバーされているかの確認。例: R1〜R5 をすべてカバー。機能↔要求の完全な対応は §4 各機能の `関連要求` を正とし、ここで繰り返さない>
- **1:1でない箇所**: <要求と機能が1:1にならない箇所だけを挙げる。なければ「すべて1:1」>
  - 例: F2 項目の登録 / F4 タグの付与 / F6 ログイン は、要求が語っていないが成立に必要な前提機能(それぞれ R1 / R2 / 全要求 を成立させるため)

## 2. 現状把握

<!-- 既存機能・既存UI/UXのうち、本機能設計に関わる部分の要旨。コードの実装詳細には踏み込まない(後続のimpact-analysisが扱う)。なければ「該当なし」 -->

- <既存画面X>: <現状の構成・動線の要旨>
- <既存機能Y>: <ユーザー体験上の振る舞いの要旨>

## 3. 評価軸

<!-- 本機能設計で重視する評価軸。共通軸は常に置き、案件固有軸があれば追加する。
案件固有軸には必ず「導出根拠」を添える(要求仕様書のどこ/背景から、なぜその軸が重要か)。
案件固有軸を立てた場合は、選定に入る前に Phase 3 でユーザーへ提示し認識を合わせる。 -->

- **ユーザー価値**: <この機能設計でのこの軸の意味合いを1行>
- **学習コスト**: <ユーザーが振る舞いを理解・習得しやすいかの観点>
- **標準/類似サービスとの揃え**: <既存慣習・類似サービスとの揃いの観点>
- **拡張性**: <要求が増えたとき壊れにくいかの観点>
- **変更の不可逆性**: <一度提供したら戻しにくいかの観点>
- **<案件固有軸>** (該当する場合): <この案件で重視する追加軸の定義> — 根拠: <要求仕様書のどの記述・背景・Why から導出したか>

## 4. 機能設計

<!-- 要求ではなく「機能」の単位で組織する。要求を1件ずつ機能化するのではなく、
「これらの要求が満たされたサービスが成立するには、どんな機能が要るか」を起点に洗い出す。
要求が語っていない前提機能(閲覧対象を登録する機能、一覧に項目を追加する機能、認証など)も、成立に不可欠なら対等な機能として並べる。
各機能には「関連要求」を添える(1機能が複数要求にまたがってよい)。 -->

### F1 <機能名>

- **関連要求**: R1, R2 <!-- 応える要求のID。複数可。要求が語っていなかった前提機能なら「(前提機能) Rn を成立させるため」のように添える -->
- **ゴール**: <この機能で何が達成されるかを1〜2行に圧縮>

#### 選定案: <案の名前>

**ユーザー操作フロー**:

1. <ユーザーが行う操作1>
2. <システムの応答(ユーザーに見える結果)>
3. ...

**画面要素** <!-- 静的な配置のみ。動的に変わる見え方は「状態のバリエーション」側に書く。主要な接点(2〜3画面)に重点を置き、それ以外は接続点だけ言及する -->:

- **<画面X>**: <要素A>, <要素B>, <ユーザーが操作可能なもの>
- **<画面Y>**: <要素群>

**状態のバリエーション** <!-- 同じ画面要素が状態に応じてどう変化するか。該当しないものは省略してよい -->:

- **ローディング中**: <ユーザーに何が見えるか>
- **空**: <ユーザーに何が見えるか>
- **エラー**: <ユーザーに何が見えるか>
- **成功**: <ユーザーに何が見えるか>

**採用理由**:

- <どの評価軸でどの却下案より優位か。1〜3点に絞る>

#### 主要却下案

<!-- 主要な対立軸を代表する案だけ残す。最大3つ目安だが、2案で十分なら2案で止める。
無理に3案目をひねり出すと「他案の劣化版」「対立軸として弱い案」が並びノイズになる。
検討の途中で出た雑多な案・弱い案は載せない。
関連要求に [決定] ラベルがある機能でベースライン案を脅かす別案が見当たらなければ「別案検討: 該当なし」と明記する。
前提機能で振る舞いがほぼ一意に定まるものは、主要却下案を省略し選定案のみを浅く書いてよい。 -->

- **案A: <名前>** — <案の概要(1〜2行)> / 却下理由: <どの評価軸で劣ったか>
- **案B: <名前>** — <案の概要(1〜2行)> / 却下理由: ...

#### 確認ポイント (あれば)

- <ユーザー判断が必要な未決点。なければセクションごと省略可>

<!-- 機能の数だけ F1, F2, … と連番で繰り返す -->

## 5. 横断的な機能設計上の決定

<!-- 複数の機能にまたがる方針(例: 通知パターンを全機能で統一、ナビゲーション全体の整合性)。なければ「該当なし」 -->

- <横断的な決定事項1>
- <横断的な決定事項2>

## 6. 後続フェーズへの引き継ぎ

<!-- 次フェーズ(impact-analysis)で扱ってほしい論点や、機能設計の確定により新たに浮上した検討事項。なければ「該当なし」 -->

- <impact-analysisで扱うべき論点>
- <implementation-designで扱うべき論点>

### 前提機能の欠落 (あれば)

<!-- 要求群が暗黙に前提しているが、スコープ/ポリシー判断が必要で本スキルでは設計しなかった土台機能。
例: 「CSV一括登録した利用者がログインする認証機能」。なぜ設計を保留したか(どの判断が要るか)を添える。なければ「該当なし」 -->

- <前提機能の欠落と、保留した理由(要する判断)>
