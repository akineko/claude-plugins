# 設計メモ: <機能名>

<!--
全セクションを必ず残す。該当する内容がない場合は、見出しの直下に「該当なし」と明記する。

このファイルは spec-toolkit/skills/quick-design スキルが生成する。小規模な機能追加・修正向けに、
要求整理〜実装設計を1ファイルに収める。## 2〜## 5 は実装設計書(implementation-design)と
同一の見出し構造であり、そのまま phase-implementation に渡せる(入口ガードは `4. 実装計画` を見る)。

実装本体のコード・内部アルゴリズム擬似コード・呼び出し例は書かない(契約レベルで止める)。
-->

## 1. スコープ

- **要求**: <What/Why を数行。誰のためかが自明でなければ添える>
- **確定事項**: <[確定] の一覧。なければ「該当なし」>
- **未決事項**: <[未決] と各々の扱い(選択肢確認で確定 / 仮置き+残課題)。なければ「該当なし」>
- **対象外**: <扱わない範囲。なければ「該当なし」>

## 2. 設計骨格

### 2.1 振る舞いの決定

<!-- 振る舞いのまとまりごとに繰り返す。案の名前と本文は「何が見え・何が起こるか」で書く -->

- **選定案: <名前>**: <操作の流れと見え方の要点>
- **採用理由**: <推奨根拠。実装コスト(既存パターンへの乗りやすさ等)を含んでよい>
- **主要却下案**: <案: 概要 / 却下理由>(なければ「該当なし」)

### 2.2 主要な意思決定

<!-- 実装骨格の決定を2〜4個目安で。詳細(列定義・シグネチャ)は 3.x に書く -->

- **<決定1>**: <要点と理由>

### 2.3 変更箇所

| パス | 役割 | 変更内容 |
|------|------|---------|
| ... | ... | ... |

- **既存コードとの齟齬・前提**: <接地調査で判明した不整合(要望の前提機能が実は無い等)。なければ「該当なし」>

## 3. 詳細設計

<!-- 「実装フェーズで設計に立ち戻らずに済む契約レベル」で書く。精読した実物に接地させる -->

### 3.1 データ構造 / DB スキーマ

- **<テーブル / 型名>**: <用途>
  - フィールド: <名: 型 (制約)>

### 3.2 API / 関数インタフェース

- **<エンドポイント / 関数名>**: <用途>
  - リクエスト/引数: <形>
  - レスポンス/戻り値: <形>
  - 主要エラー: <型/ステータス>

### 3.3 主要な動作フロー

- **<フロー名>**: <入力 → 処理 → 出力。5ステップ以内>

### 3.4 エラーハンドリング / エッジケース

- **<条件1>**: <対応方針>

## 4. 実装計画

<!--
動くもの単位のフェーズ分割(1〜3フェーズ目安)。
「型だけ定義」「ファイルだけ作る」のような中間状態はフェーズにしない。
テストを先に書くか後に書くかなどの実装プロセスは実装者の裁量であり、ここでは規定しない。
-->

### フェーズ 1: <フェーズ名>

- **目的**: <このフェーズで何が動くようになるか>
- **テスト方針**: <完了を固定するテストのレベル: ユニット/統合/契約 など>
- **実装範囲**: <どの契約を実装するか>
- **完了条件**: <該当テストが pass + 動作確認>

## 5. 残課題・後続検討事項

<!-- 仮置きのまま進めた未決(仮置き内容と、決まったら差し替える箇所)、範囲外の論点。なければ「該当なし」 -->

- <論点1>
