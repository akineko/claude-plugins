<!--
RATIONALE.md テンプレート（スキル / エージェント共通）

これは「設計ノート」の構造の正準的な出所。新規作成時はこれをコピーして埋める。
運用ルールの全体は リポジトリルート CLAUDE.md「スキル・エージェントの設計ノート（RATIONALE.md）」を参照。

書くときの原則:
- SKILL.md / エージェント定義の内容を再掲しない。「何をするか」はあちら、ここは「なぜこの形か」。
- 各セクションは該当が無ければ省略してよい(空欄を機械的に埋めない)。記録する価値があるのは「定義を読んだだけでは分からない」決定・理由・解釈だけ。
- 「中核的な設計判断」には、一見冗長・恣意的でも消すと壊れる earned wisdom を必ず入れる(再設計時の地雷マップ)。消すと壊れる判断には行頭に【load-bearing】を付け、再設計時に grep で拾えるようにする(目印はこの表記に統一する)。
- 解釈・仮定には ⚠️ を付け、ユーザーが意図とのズレを拾えるようにする。
- 見出しは対象に合わせて改名・追加してよい(例: エージェントなら「目的と背景」→「役割と位置づけ」)。下のセクションは型ではなく、記録すべき情報の目安。
- 置き場所: スキルは <skill-name>/RATIONALE.md、エージェントは agents/<name>.RATIONALE.md。

エージェントの場合の補足:
- 定義ファイル自身が「なぜ存在するか」を内包しているので、それは参照に留め、保守者目線の決定(load-bearing な制約・出所・検証で確認した挙動)に絞る。
- そのエージェントが特定スキルのオーケストレーションの一部なら、全体設計はそのスキルの RATIONALE を参照させる。
-->

# <name> 設計ノート

> SKILL.md=「何をするか（指示）」/ 本ファイル=「なぜこの形か（意図・解釈・却下案・守る失敗）」。実行時は非読込。更新時に改訂する。SKILL.md は再掲せず参照に留める。
<!-- エージェント用なら1行目を「定義=<name>.md / 本ファイル=保守者向けの決定の出所・load-bearing な制約。定義の再掲はしない。」に置き換える -->

## 目的と背景

<!-- 解きたい問題・ユーザーが伝えた背景・パイプライン上の位置づけ。簡潔に。 -->

## 中核的な設計判断

<!-- 非自明な判断ごとに1ブロック。自明な選択は書かない。 -->

### <判断のタイトル>
- **決定**: <SKILL.md に反映されている形>
- **却下した代替案**: <案と却下理由。なければ省略>
- **防いでいる失敗**: <この判断が無いと起きること。eval で実証済みならその旨(eval名)>
- **出所**: <ユーザー要望(YYYY-MM) / eval名 / 境界チェック / 設計議論 等>
<!-- 「消すと壊れる earned wisdom」は判断タイトルの行頭に【load-bearing】を付ける(再設計時に grep で拾える) -->

## 解釈・仮定（要確認）

<!-- 曖昧な要望をどう解釈してスキルに反映したか。ユーザーがここを見て意図とのズレを直せる。 -->

- **<論点>**: <ユーザーが言ったこと(要約)> → <こう解釈してスキルに反映した>。⚠️<不確実な点・別解釈の可能性があれば>

## 改訂ログ

<!-- 大きな方針転換のみ1行。細かい修正は書かない。 -->

- YYYY-MM-DD: <何を・なぜ変えたか。保持した earned wisdom があればその旨>
