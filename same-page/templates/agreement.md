<!--
作業合意ファイルのテンプレート（same-page プラグインのフォーマット正準）

- readback が `docs/agreements/YYYY-MM-DD-<slug>.md` として生成し、
  fulfill（決定台帳）・signoff（status/確認列）・handoff（現在地）が更新する。
- 生成時は HTML コメントをすべて削除し、<> のプレースホルダを実内容に置き換える。
  frontmatter（--- ブロック）が生成ファイルの先頭に来るようにする。
  「種別の凡例」だけは本文として残す（台帳を後から読むときに必要）。
- セクションの増減はしない。書くことがないセクションは「なし」と明記する
  （空欄は「未検討」と「検討したが該当なし」の区別がつかないため）。
- 合意全体はユーザーが1〜2分で読み切れる分量に保つ。各セクションは要点の
  箇条書きのみで、背景説明や実装詳細で膨らませない（長い合意は読まれず、
  ズレ検出器として機能しない）。
-->
---
status: active
created: <YYYY-MM-DD>
updated: <YYYY-MM-DD>
---

# 作業合意: <タイトル>

<!-- status: active（作業中）| done（完了・signoff が更新）| dropped（破棄・理由を目的の下に1行残す） -->

## 目的

<!-- なぜこの作業をするか。成果物ではなく、解決したい問題を 1〜3 行で。
     ユーザーが目的を語っていない場合は推定して書き、末尾に（推定）を付ける。 -->

## やること

<!-- 合意した成果物・変更の範囲。箇条書き。 -->

## やらないこと

<!-- 明示的にスコープ外とした周辺作業。各項目に（理由）を添える。
     ズレの大半はタスクの縁で起きるため、「やること」と同等に重要。 -->

## 完了条件

<!-- 検証可能な形で番号付き。各条件に（検証: <方法>）を添える。
     signoff はこのリストの通りに検証するので、ここに書けない条件は合意できていない。 -->

1. <条件>（検証: <方法>）

## 確定事項

<!-- リードバックの質疑でユーザーと確定した解釈・選択。番号付き、選択の理由を1行添える。
     後から「なぜこうなっているのか」を辿る出発点。 -->

1. <確定した内容>（理由: <1行>）

## 前提

<!-- ユーザーに確認せず宣言した仮定。実装中に崩れたら fulfill が逸脱プロトコルで更新する。 -->

- <前提>

## 決定台帳

<!-- 作業中に行った裁量判断・前提の更新・合意の変更を追記する（新しい行を上に）。
     「確認」列は ✔（ユーザーに報告済み）/ 空欄（未報告）。signoff が未報告分を開示する。 -->

| 日付 | 決定 | 種別 | 経緯 | 確認 |
|------|------|------|------|------|

種別の凡例: `裁量` = 合意が定めない実装詳細を選択（L0）/ `前提更新` = 前提が事実と違ったが完了条件は不変（L1）/ `合意変更` = やること・完了条件が変わった（L2、ユーザー承認必須）

## 現在地

<!-- handoff が更新する。次のセッションはここだけ読めば再開できる粒度で書く。 -->

- 進捗: （未記入 — handoff が更新する）
- 次の一手:
- 開いている問題:
- 注意（ハマりどころ）:
