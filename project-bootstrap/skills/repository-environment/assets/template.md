# リポジトリ・開発環境設計書

## 1. リポジトリ構成

### 1.1 構成方針

- **採用**: [モノレポ / シングルリポジトリ / マルチリポ]
- **選定理由**: [プロジェクト特性と合致する点。アプリ数・共通パッケージの有無・チーム編成]
- **他候補**: [候補名と落選理由]
- **採用ツール**: [モノレポ採用時のみ。pnpm workspaces / Turborepo / Nx / yarn workspaces 等]

### 1.2 モノレポのトップレベル構成

> モノレポを採用しない場合は「該当なし」と記載してこのセクションを削除してよい。詳細なディレクトリ規約はディレクトリ構成設計書（`docs/directory-structure.md`）で扱う。

```
.
├── apps/
│   ├── [...]
│   └── [...]
├── packages/
│   ├── [...]
│   └── [...]
└── tools/   # 必要なら
```

- **`apps/` の方針**: [デプロイ単位のアプリケーション。Web / API / CLI 等]
- **`packages/` の方針**: [共通ライブラリ。型定義 / UI コンポーネント / ロジック等]
- **その他のトップレベル**: [必要なら。tools/, scripts/, infra/ 等]

## 2. ツールチェイン

### 2.1 ランタイム・バージョン管理

- **ランタイム**: [Node.js 20 LTS / Python 3.12 / Go 1.22 等]
- **バージョン固定方法**: [Volta / mise / asdf / .nvmrc / .tool-versions]
- **理由**: [...]

### 2.2 パッケージマネージャ

- **採用**: [npm / pnpm / yarn / bun / uv / Poetry / cargo 等]
- **選定理由**: [モノレポ workspace、速度、ロックファイル運用]
- **他候補**: [候補名と落選理由]

### 2.3 ビルドツール

- **採用**: [Vite / Turbopack / Webpack / esbuild / フレームワーク標準 等]
- **選定理由**: [フレームワーク親和性、ビルド速度、設定の簡潔さ]

### 2.4 タスクランナー

- **採用**: [npm scripts / Turborepo / Nx / Make / Just 等]
- **選定理由**: [モノレポでの依存解決、キャッシュ、並列実行]

## 3. コード品質ツール

### 3.1 Linter

- **採用**: [Biome / ESLint / Oxlint / Ruff / golangci-lint / clippy 等]
- **設定の出発点**: [recommended プリセット / フレームワーク標準 / 独自基準]
- **自動修正の運用**: [保存時 / pre-commit / 手動]

### 3.2 Formatter

- **採用**: [Biome / Prettier / Ruff format / gofmt / rustfmt 等]
- **設定の出発点**: [...]
- **適用範囲**: [対象拡張子]

> Biome を採用する場合は 3.1 と 3.2 を一本化（「3.1/3.2 ともに Biome」と記載）。

### 3.3 型チェッカ

- **採用**: [TypeScript / mypy / pyright / 該当なし]
- **strict 設定**: [strict / strictNullChecks / noUncheckedIndexedAccess の有無]
- **検証タイミング**: [エディタ / pre-commit / CI]

### 3.4 共通設定の管理方針

> シングルリポジトリの場合は「該当なし」と記載。

- **方針**: [共通 config パッケージ化 / ルートに集約してパッケージから extends / 各パッケージで複製]
- **対象**: [tsconfig / lint config / format config 等]

## 4. Git 運用

### 4.1 ブランチ戦略の概要

> 詳細（環境連動、CI トリガー）はインフラ設計書「4.2 リリース戦略」と整合させる。

- **方式**: [trunk-based / GitHub Flow / Git Flow]
- **保護ブランチ**: [main / 必要に応じて release/*]
- **PR ベースの運用**: [必須 / オプション、レビュー必須有無]

### 4.2 コミットメッセージ規約

- **採用規約**: [Conventional Commits / 独自 / なし]
- **type 一覧**: [feat / fix / docs / chore / refactor / test / ci 等]
- **scope の使い方**: [必須 / オプション / 該当なし]
- **検証**: [本人運用 / commitlint / CI]

### 4.3 .gitignore 方針

必ず除外する種別:

- **環境変数**: `.env`, `.env.*`（`.env.example` は除く）
- **依存関係**: `node_modules/`, `.venv/` 等
- **ビルド成果物**: `dist/`, `build/`, `.next/`, `out/` 等
- **エディタ・OS**: `.vscode/*` のうち共有しないもの、`.DS_Store`
- **その他**: [プロジェクト固有の除外対象]

## 5. Git Hooks

### 5.1 採用ツール

- **採用**: [Husky / lefthook / simple-git-hooks / pre-commit (Python) 等]
- **選定理由**: [導入の簡単さ、設定ファイルの形式、依存関係]

### 5.2 pre-commit で実行する内容

- **対象ファイル**: [変更分のみ / 全体]
- **実行ジョブ**:
  - [Linter / Formatter（変更ファイルに対して自動修正 + 検証）]
  - [型チェック（軽量に走らせるか CI に任せるか）]
  - [シークレット検出（gitleaks / detect-secrets 等）]
  - [テスト（必要なら高速なものだけ）]

> セキュリティ設計書「5.1 シークレット管理」と整合させる。シークレット検出は pre-commit に組み込むことを推奨。

### 5.3 commit-msg で実行する内容

- **規約検証**: [commitlint / 独自スクリプト / 該当なし]

## 6. エディタ・開発体験

### 6.1 .editorconfig

- **採否**: [採用 / 該当なし]
- **主要設定**: インデント幅、改行コード（LF）、末尾改行、文字コード（UTF-8）

### 6.2 推奨拡張機能

`.vscode/extensions.json` に記載する主要項目:

- [Linter 拡張]
- [Formatter 拡張]
- [フレームワーク特化拡張]
- [その他チーム共通で揃えたいもの]

### 6.3 開発コンテナ

- **採否**: [採用 / 該当なし]
- **理由**: [採用するならローカル環境差異の解消、ツールバージョンの統一など]

## 7. ローカル環境変数管理

> 本番のシークレット保管先はインフラ設計書「8.1 シークレットの保管先」、保管方針はセキュリティ設計書「5.1 シークレット管理」を参照。本書はローカル開発時の運用のみを扱う。

### 7.1 .env 運用方針

- **ファイル名**: [`.env`, `.env.local`, `.env.development` 等の使い分け]
- **コミット禁止**: `.gitignore` で除外（5.x の Hooks でシークレット検出を併用）
- **ロード方法**: [フレームワーク標準 / dotenv / direnv 等]
- **複数環境**: [必要なら、ローカルでの dev/prod 切替方法]

### 7.2 .env.example の管理

- **方針**: [必要なキーの一覧をダミー値付きで管理]
- **同期**: [新しいキーを追加した際に `.env.example` も更新するルール]

## 付録: ADR候補

[本書で行われた重要な判断のうち、ADR 化すべきものをリストする]

- [ ] [リポジトリ構成判断（モノレポ採用 等）] — 理由: [変更コスト / 議論の余地 / 影響範囲]
- [ ] [パッケージマネージャ選定] — 理由: [...]
- [ ] [Linter / Formatter 選定] — 理由: [...]

## 付録: 保留事項

[該当セクションの作成時に保留にした項目をここに記録する。全項目確定時は「なし」と記載]

<!--
記録フォーマット:

### [セクション番号] 項目名

- **未確定内容**: 何が決まっていないか
- **必要な情報 / 確認事項**: 確認すべきこと、調査が必要な点
- **補足**: ここまでに集まった情報・前提・候補案
-->
