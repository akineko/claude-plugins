# ディレクトリ構成設計書

## 1. 対象パッケージと前提

### 1.1 対象パッケージ

- **パッケージパス**: [例: `apps/web` / `apps/api` / `packages/shared` / シングルリポジトリならルート]

### 1.2 採用フレームワーク・パッケージ種別

- **パッケージ種別**: [FE app / BE app / 共通ライブラリ / CLI ツール / その他]
- **フレームワーク・主要ライブラリ**: [例: Next.js 14 (App Router), Hono, FastAPI 等]
- **言語**: [TypeScript / Python / Go / Rust 等]

## 2. ディレクトリ構成方針

### 2.1 構成スタイル

- **採用**: [feature-based / layer-based / フレームワーク慣習 / ハイブリッド]
- **選定理由**: [パッケージ種別・フレームワーク・想定する開発スタイルとの合致点]
- **「feature」「layer」の単位の定義**: [feature-based なら「業務機能 / 画面 / ドメイン」のどれを feature とするか。layer-based ならどんな層に分けるか]
- **他候補**: [候補名と落選理由]

### 2.2 トップレベル構成

`src/` 配下（または言語慣習の起点）の主要ディレクトリ:

```
src/
├── [...]
├── [...]
└── [...]
```

各ディレクトリの役割（1 行ずつ）:

- **`[ディレクトリ名]/`**: [...]
- **`[ディレクトリ名]/`**: [...]
- **`[ディレクトリ名]/`**: [...]

### 2.3 サブディレクトリ規約

> feature 単位 / モジュール単位の内部構成。`features/<feature>/` や `modules/<module>/` の中身、または layer 内部の構成。

```
[features/<feature>/]
├── [...]
└── [...]
```

- **共通要素の置き場所**: [複数の feature で使うコンポーネント・ユーティリティはどこに置くか]
- **共通化の閾値**: [何回再利用されたら共通に上げるか、判断軸]
- **feature 間の参照**: [禁止 / 許可 / 制限あり]

## 3. ファイル命名規約

### 3.1 ファイル名の形式

| 種別 | 命名規約 | 例 |
|---|---|---|
| [コンポーネント] | [PascalCase / kebab-case] | [...] |
| [フック] | [...] | [...] |
| [ユーティリティ関数] | [...] | [...] |
| [型定義] | [...] | [...] |
| [ルート / エンドポイント] | [...] | [...] |
| [テスト] | [...] | [...] |

### 3.2 拡張子の使い分け

| 拡張子 | 用途 |
|---|---|
| [`.ts`] | [型のみ・ロジックのみ] |
| [`.tsx`] | [JSX を含むコンポーネント] |
| [`.test.ts(x)`] | [ユニット・インテグレーションテスト] |
| [`.spec.ts(x)`] | [採用しない / E2E に限定 等] |
| [`.stories.tsx`] | [Storybook を採用する場合] |

## 4. テストファイルの配置

- **配置方針**: [コロケーション / `__tests__/` / `tests/` トップレベル / 言語慣習に従う]
- **ユニット・インテグレーション**: [...]
- **E2E**: [`e2e/` / `tests/e2e/` 等]
- **テストヘルパー**: [`src/test-utils/` / `tests/helpers/` 等]
- **テストデータ（fixtures）**: [配置先]

> テスト戦略書「2. テストレベル」「5. テストデータとモック」と整合させる。

## 5. インポート規約

### 5.1 パスエイリアス

| エイリアス | 解決先 | 用途 |
|---|---|---|
| [`@/`] | [`src/`] | [パッケージ内部の絶対参照] |
| [`@workspace/<pkg>`] | [モノレポ内パッケージ] | [モノレポ採用時のパッケージ間参照] |

> tsconfig（または対応する設定ファイル）の `paths` と一致させる。リポジトリ・開発環境設計書「3.3 型チェッカ」と整合させる。

### 5.2 相対パス vs 絶対パスの使い分け

- **絶対パス（エイリアス）を使う場面**: [...]
- **相対パスを使う場面**: [...]
- **判断軸**: [例: 同一ディレクトリ内・同一 feature 内は相対、それ以外は絶対]

## 付録: ADR候補

[本書で行われた重要な判断のうち、ADR 化すべきものをリストする]

- [ ] [構成スタイル選定（例: feature-based 採用）] — 理由: [変更コスト / 議論の余地 / 影響範囲]
- [ ] [テストファイル配置の特殊な選択（コロケーション以外を採用した場合）] — 理由: [...]
- [ ] [パスエイリアス設計（複数エイリアスや特殊な命名を採用した場合）] — 理由: [...]

## 付録: 保留事項

[該当セクションの作成時に保留にした項目をここに記録する。全項目確定時は「なし」と記載]

<!--
記録フォーマット:

### [セクション番号] 項目名

- **未確定内容**: 何が決まっていないか
- **必要な情報 / 確認事項**: 確認すべきこと、調査が必要な点
- **補足**: ここまでに集まった情報・前提・候補案
-->
